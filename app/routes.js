module.exports = function(app, api, details) {

	// PAGE ROUTES =============================================================
	// home
	app.get('/', function(req, res) {
	    res.render('../views/index.html');
	});

	// API ROUTES =============================================================

}